version: 2.0

jobs:
  "ogame":
    docker:
      - image: markhobson/maven-chrome:jdk-11
    working_directory: ~/ogame
    environment:
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - run:
          name: OGAME
          command: mvn test -Dsuite=Regression -Ddriver.type=CHROME -Dog.url=https://s166-pl.ogame.gameforge.com/ -Dog.user=triko2020@gmail.com -Dog.pass=qweasdzxc

workflows:
  version: 2
  commit:
    triggers:
      - schedule:
          cron: "30 * * * *"
          filters:
            branches:
              only: master
    jobs:
      - "ogame"